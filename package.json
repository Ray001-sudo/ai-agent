{
  "name": "tenderpro-ai-agent",
  "version": "1.0.0",
  "description": "TenderPro — Autonomous Global Tender Procurement AI Agent",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && node server.js\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js",
    "seed": "cd backend && node ../scripts/seed.js",
    "docker:up": "docker-compose up -d --build",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f backend",
    "docker:reset": "docker-compose down -v && docker-compose up -d --build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
